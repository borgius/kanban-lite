# Workspace instruction bridge

Repo-wide defaults live in `/AGENTS.md`.

Detailed, lower-noise guidance now lives in `.github/instructions/` and `.agents/skills/` so file-specific and workflow-specific rules load only when relevant.
