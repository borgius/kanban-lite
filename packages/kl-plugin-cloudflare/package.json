{
  "name": "kl-plugin-cloudflare",
  "version": "0.1.0",
  "description": "kanban-lite Worker-safe Cloudflare bundle for card storage, attachments, card state, and config storage",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/index.cjs",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.cjs",
      "default": "./dist/index.cjs"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "KANBAN_PLUGIN_PACKAGE_DIR=$PWD pnpm --dir ../.. exec vite build --config scripts/vite.plugin-package.config.mts",
    "watch": "KANBAN_PLUGIN_PACKAGE_DIR=$PWD pnpm --dir ../.. exec vite build --watch --config scripts/vite.plugin-package.config.mts",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "release": "npm run clean && npm run build && npm version patch && npm publish",
    "release:minor": "npm run clean && npm run build && npm version minor && npm publish",
    "release:major": "npm run clean && npm run build && npm version major && npm publish"
  },
  "peerDependencies": {
    "kanban-lite": ">=1.0.0"
  },
  "peerDependenciesMeta": {
    "kanban-lite": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  },
  "keywords": [
    "kanban-lite",
    "kanban",
    "cloudflare",
    "d1",
    "r2",
    "storage",
    "plugin"
  ]
}
