{
  "name": "kl-plugin-storage-postgresql",
  "version": "0.1.0",
  "description": "kanban-lite card.storage and attachment.storage plugin for PostgreSQL",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/index.cjs",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.cjs",
      "default": "./dist/index.cjs"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "KANBAN_PLUGIN_PACKAGE_DIR=$PWD pnpm --dir ../.. exec vite build --config scripts/vite.plugin-package.config.mts",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:integration": "vitest run src/postgresql-kanban-lite.workspace.test.ts",
    "test:integration:service": "vitest run src/index.integration.test.ts",
    "release": "npm run clean && npm run build && npm version patch && npm publish",
    "release:minor": "npm run clean && npm run build && npm version minor && npm publish",
    "release:major": "npm run clean && npm run build && npm version major && npm publish"
  },
  "peerDependencies": {
    "pg": ">=8.0.0",
    "kanban-lite": ">=1.0.0"
  },
  "peerDependenciesMeta": {
    "pg": {
      "optional": false
    },
    "kanban-lite": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/pg": "^8.11.0",
    "pg": "^8.13.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  },
  "keywords": [
    "kanban-lite",
    "kanban",
    "postgresql",
    "postgres",
    "storage",
    "plugin"
  ]
}
